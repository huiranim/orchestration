# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 현재 상태

이 저장소는 아직 **설계 문서만** 존재한다. 구현 코드, 빌드 시스템, 테스트, git 이력이 없다.
유일한 콘텐츠는 과제 설계 명세서다:

- `docs/specs/2026-06-04-orchestration-homework-design.md` — 전체 과제 목적, 비교 분석, 시나리오, 8주 일정의 단일 출처(source of truth)

코드를 작성하기 전에 위 명세서를 먼저 읽을 것. 아래 내용은 그 명세서의 요약이며, 충돌 시 명세서가 우선한다.

## 과제 목적

데이터 플랫폼 오케스트레이션 학습 과제다. **Airflow와 Dagster로 동일한 파이프라인을 각각 구현·비교**하여 도구를 선택하고, 우선순위 선점·동시성 제어·의존성 전파·선택적 재실행을 직접 구현한다. 최종적으로 사내 자체 오케스트레이터 `didimdp_manager`의 한계(FIFO 우선순위 부재, 불안정한 동시성 제어, 선택적 재실행 미지원, 제한적 크로스 DAG 의존성)에 대한 개선안을 도출한다.

## 핵심 설계 제약 (구현 시 반드시 준수)

- **언어**: Python (PySpark). Airflow/Dagster DAG 코드와 언어 통일.
- **Spark 실행**: Local 모드 (`spark.master=local[*]`). 클러스터 구성 없음.
- **태스크는 전부 스텁**: 실제 비즈니스 로직 없이 `time.sleep`으로 처리 시간을 시뮬레이션하고 플래그로 성공/실패를 결정한다. 단, **실제 `SparkSession`은 생성**하여 오케스트레이터가 Spark Job을 실제로 제어하는 흐름을 유지한다. (명세서 §4-1 스텁 예시 참조)
- **Spark 슬롯 한계 = 3개**. 오케스트레이터의 Pool/Concurrency 설정으로 강제하는 **논리적 제약**이며 실제 Spark 자원 사용량과 1:1 대응하지 않는다. 3개는 P1(1)+P3(1)+여분(1)로, 여분 슬롯이 긴급 실행 즉시성을 보장하도록 의도적으로 설계됨.

## 도메인 모델: 4개 파이프라인

| 파이프라인 | 주기 | 우선순위(weight) | 슬롯 |
|---|---|---|---|
| P1. 주문 수집 | 매시간 정각 | 높음 (2) | 1 |
| P2. 매출 집계 | P1 완료 후 매시간 | 중간 (1) | 1 |
| P3. 정산 배치 | 매일 자정 + 긴급 수동 트리거 | 최고 (3) | 1 |
| P4. 추천 모델 학습 | 매주 월 02:00 | 낮음 (0) | 2 |

- 의존성 체인: **P1 전체 성공 → P2 실행**. P1의 T3(DW 적재) 실패는 P2·P3를 차단한다.
- P1 T3, P4 T3는 각각 시나리오 C·A의 핵심 대상이라 단독 제어가 가능해야 함. (각 태스크 구성은 명세서 §4-2 참조)

## 검증 대상 3대 시나리오

구현의 정합성은 이 세 시나리오를 재현·검증할 수 있는지로 판단한다 (명세서 §4-3):

- **시나리오 A (우선순위 선점)**: P4 T3(슬롯 2) 실행 중 P3 긴급 트리거 → P3가 P4보다 슬롯을 먼저 확보하는가.
- **시나리오 B (동시성 제어)**: 총 요청 슬롯 5개 > 한계 3개 상황에서, 슬롯 반납 시 대기열이 **우선순위 순서**로 소비되는가.
- **시나리오 C (의존성 + 선택적 재실행)**: P1 T3 실패로 P2·P3 차단 → **T3만** 재실행(T1·T2 제외) 성공 시 P2·P3가 자동 unblock되는가.

## 구현 순서 (명세서 §5 일정 기반)

2주차에 **두 도구로 미니 파이프라인(P1)을 각각 구현해 비교 후 한 도구를 선택**하고, 3주차부터 선택한 도구로만 전체 시나리오를 구현한다. 따라서 초기에는 `airflow/`와 `dagster/`를 병행하다가 이후 한쪽으로 수렴하는 구조를 예상할 수 있다.

- Airflow 로컬 셋업: Docker Compose (Postgres, Redis, Worker). 우선순위는 `priority_weight` + Pool, 크로스 DAG 의존성은 `ExternalTaskSensor`, Spark 연동은 `SparkSubmitOperator`.
- Dagster 로컬 셋업: `dagster dev`. 우선순위는 `priority` + Concurrency limit, 의존성은 Asset 간 lineage, Spark는 `@op` 내 직접 호출.
